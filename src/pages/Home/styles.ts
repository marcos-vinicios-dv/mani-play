import styled from 'styled-components';

export const Container = styled.main`
  padding: 1.6rem 2.15rem;

  h2 {
    color: var(--gray-700);

    margin-top: 2rem;
  }

  > span {
    font-weight: 600;
    color: var(--gray-500);
  }

  ul {
    width: 100%;
    padding: 1rem 0;
  }
`;
